# Pre-commit 組み込み観点

commit 時に回す前提では、次を確認するにゃ。

- 実行速度が commit のたびに回しても現実的か
- `--fix` や `--write` のような自動修正が使いやすいか
- staged file だけに適用しやすいか
- CI でも同じコマンドを再利用しやすいか
- formatter と linter を別々に動かすほうが自然か、1 ツールにまとめるほうが自然か

重すぎる構成や、commit 時の体験を大きく悪化させる構成は避けるにゃ。
